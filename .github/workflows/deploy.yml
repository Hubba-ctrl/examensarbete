name: Deploy webserver

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run ansible playbook
        run: ansible-playbook -i inventory.ini webserver.yaml
